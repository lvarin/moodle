Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}
{{ if .Values.URL }}
URL: https://{{ .Values.URL }}
{{ else }}
No URL provided in "values.yaml". To get the autogenerated URL run:

    $ oc describe route
{{ end }}
  Admin User: {{ .Values.moodle.user }}
  Admin Password: {{ .Values.moodle.password }}

The initialization of the application may take a couple of minutes
